import { SortByDistancePipe } from './sort-by-distance.pipe';

describe('SortByDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
