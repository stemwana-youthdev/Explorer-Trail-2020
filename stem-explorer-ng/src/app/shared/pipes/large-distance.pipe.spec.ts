import { LargeDistancePipe } from './large-distance.pipe';

describe('LargeDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new LargeDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
