export interface Profile {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  region: string;
  homeTown: string;
  userId: string;
  photoUrl: string;
  profileCompleted: boolean;
}
